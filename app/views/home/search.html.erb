<!-- A query was attempted -->
<% if params.has_key?(:location) || (params.has_key?(:latitude) && params.has_key?(:longitude))%>
	<% @query = search_params(params)["businesses"] %>
	<!-- No results -->
	<% if @query == nil %>
		<%= render 'invalid' %>
	<% elsif @query != nil %>
		<% @results = @query %>
		<% if @results != nil %>
			<%= render 'results' %>
		<% else %>
			<%= render 'invalid' %>
		<% end %>
	<% end %>

<!-- Fill out a form -->
<% else %>
	<%= render 'form' %>
<% end %>
